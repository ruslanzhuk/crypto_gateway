{% macro styled_row(field) %}
    {{ form_row(field, {
        attr: {
            class: 'w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-pink-400 focus:border-pink-400 text-sm'
        }
    }) }}
{% endmacro %}

{% import _self as forms %}

{{ form_start(form, { attr: { class: 'space-y-10' } }) }}

<!-- Основна інформація -->
<div class="bg-pink-50 p-6 rounded-xl border border-pink-200 shadow-sm">
    <h2 class="text-lg font-semibold text-pink-800 mb-4">🧾 Основна інформація</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {{ forms.styled_row(form.user) }}
        {{ forms.styled_row(form.wallet) }}

        <div class="col-span-1 md:col-span-2">
            <label class="block text-sm font-medium text-gray-700 mb-1">
                {{ form_label(form.txHash) }}
            </label>
            <div class="bg-white border border-gray-300 rounded px-3 py-3 text-sm font-mono overflow-auto max-w-full whitespace-nowrap">
                {{ form_widget(form.txHash, {
                    attr: {
                        class: 'w-full border-0 p-0 bg-transparent text-sm font-mono'
                    }
                }) }}
            </div>
            {% if form_errors(form.txHash) %}
                <p class="text-red-500 text-xs mt-1">{{ form_errors(form.txHash) }}</p>
            {% endif %}
        </div>

        {{ forms.styled_row(form.expiredAt) }}
    </div>
</div>

<!-- Валюти -->
<div class="bg-gray-50 p-6 rounded-xl border border-gray-200">
    <h2 class="text-lg font-semibold text-gray-700 mb-4">💰 Валюти</h2>
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {{ forms.styled_row(form.fiatCurrency) }}
        {{ forms.styled_row(form.cryptoCurrency) }}
        {{ forms.styled_row(form.amountFiat) }}
        {{ forms.styled_row(form.amountCrypto) }}
        {{ forms.styled_row(form.receivedAmountFiat) }}
        {{ forms.styled_row(form.receivedAmountCrypto) }}
    </div>
</div>

<!-- Статуси -->
<div class="bg-pink-100 p-6 rounded-xl border border-pink-200">
    <h2 class="text-lg font-semibold text-pink-900 mb-4">📊 Статуси</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {{ forms.styled_row(form.mainStatus) }}
        {{ forms.styled_row(form.manualStatus) }}
        {{ forms.styled_row(form.automaticStatus) }}
        {{ forms.styled_row(form.confirmation) }}
    </div>
</div>

<!-- Опції -->
<div class="bg-gray-100 p-6 rounded-xl border border-gray-300">
    <h2 class="text-lg font-semibold text-gray-800 mb-4">⚙️ Опції</h2>
    <div class="flex items-center space-x-3">
{{ form_row(form.isAutomatic, {
    row_attr: { class: 'flex items-center space-x-2' },
    attr: { class: 'h-5 w-5 text-pink-500 focus:ring-pink-400' }
}) }}