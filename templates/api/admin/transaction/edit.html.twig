{% extends 'api/api_base.html.twig' %}

{% block title %}Edit Transaction{% endblock %}

{% block body %}
    <div class="max-w-4xl mx-auto mt-10 bg-white shadow-lg rounded-xl p-6">
        <h1 class="text-2xl font-bold mb-6 text-gray-800">Edit Transaction</h1>

        <div class="mb-6">
            {{ include('api/admin/transaction/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="flex justify-between items-center">
            <a href="{{ path('app_transaction_index') }}" class="text-sm text-blue-600 hover:underline">
                ← Back to list
            </a>

            <div>
                {{ include('api/admin/transaction/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}