{#<!DOCTYPE html>#}
{#<html lang="en">#}
{#<head>#}
{#    <meta charset="UTF-8">#}
{#    <title>{% block title %}Admin API{% endblock %}</title>#}
{#    <meta name="viewport" content="width=device-width, initial-scale=1">#}

{#    #}{# Tailwind CSS CDN (якщо без компіляції) #}
{#    <script src="https://cdn.tailwindcss.com"></script>#}

{#    {% block stylesheets %}{% endblock %}#}
{#</head>#}
{#<body class="bg-gray-50 text-gray-800 min-h-screen">#}
{#<nav class="bg-white border-b shadow-sm p-4">#}
{#    <div class="container mx-auto flex justify-between items-center">#}
{#        <h1 class="text-xl font-bold">🔐 Crypto Gateway Admin</h1>#}
{#        <div>#}
{#            <a href="{{ path('app_transaction_index') }}" class="text-sm text-blue-600 hover:underline mr-4">Transactions</a>#}
{#            <a href="{{ path('app_user_index') }}" class="text-sm text-blue-600 hover:underline">Users</a>#}
{#        </div>#}
{#    </div>#}
{#</nav>#}

{#<main class="p-6">#}
{#    {% block body %}{% endblock %}#}
{#</main>#}

{#{% block javascripts %}{% endblock %}#}
{#</body>#}
{#</html>#}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Admin Panel{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-slate-100 via-slate-200 to-slate-100 min-h-screen text-slate-800">
<div class="flex min-h-screen">
    <!-- Sidebar -->
    <aside class="w-64 bg-white shadow-lg p-4 hidden md:block">
        <div class="text-2xl font-bold mb-6">Admin Dashboard</div>
        <nav class="flex flex-col gap-2">
            <a href="{{ path('app_user_index') }}" class="hover:bg-blue-100 p-2 rounded transition">Users</a>
            <a href="{{ path('app_transaction_index') }}" class="hover:bg-blue-100 p-2 rounded transition">Transactions</a>
            <a href="{{ path('app_wallet_index') }}" class="hover:bg-blue-100 p-2 rounded transition">Wallets</a>
        </nav>
    </aside>

    <!-- Main content -->
    <main class="flex-1 p-6">
        <div class="bg-white shadow-md rounded-xl p-6">
            {% block body %}{% endblock %}
        </div>
    </main>
</div>
</body>
</html>