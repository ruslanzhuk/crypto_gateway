{% extends 'dashboard/dashboard_base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <h1 class="text-2xl font-semibold text-gray-800 mb-6">📊 Main Dashboard</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <!-- Transactions Card -->
        <a href="{{ path('app_transaction_index') }}" class="bg-white shadow-md hover:shadow-lg border border-gray-200 rounded-xl p-6 transition duration-200 hover:bg-purple-50">
            <div class="text-purple-700 text-xl font-bold mb-2">💸 Transactions</div>
            <p class="text-sm text-gray-600">View and manage all transactions in the system.</p>
        </a>

        <!-- Wallets Card -->
        <a href="{{ path('app_wallet_index') }}" class="bg-white shadow-md hover:shadow-lg border border-gray-200 rounded-xl p-6 transition duration-200 hover:bg-purple-50">
            <div class="text-purple-700 text-xl font-bold mb-2">👛 Wallets</div>
            <p class="text-sm text-gray-600">List and manage user wallets and balances.</p>
        </a>

        {% if is_granted('ROLE_SUPERADMIN') %}
            <!-- Users Card -->
            <a href="{{ path('app_user_index') }}" class="bg-white shadow-md hover:shadow-lg border border-gray-200 rounded-xl p-6 transition duration-200 hover:bg-purple-50">
                <div class="text-purple-700 text-xl font-bold mb-2">👤 Users</div>
                <p class="text-sm text-gray-600">Administer system users and permissions.</p>
            </a>
        {% endif %}

        <!-- Bots Card -->
        <a href="{{ path('app_bot_manager') }}"
           class="relative bg-white shadow-md hover:shadow-lg border border-gray-200 rounded-xl p-6 transition duration-200 hover:bg-purple-50">

            {# бейдж у правому верхньому кутку #}
            <span class="absolute top-2 right-2 inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-purple-100 text-purple-700">
                {{ botsCount|default(0) }}
            </span>

            <div class="text-purple-700 text-xl font-bold mb-2">🤖 Bots</div>
            <p class="text-sm text-gray-600">
                Manage your Telegram bots — create, verify and activate bots for payments.
            </p>
        </a>
    </div>
{% endblock %}
