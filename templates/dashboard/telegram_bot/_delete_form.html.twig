<form method="post" action="{{ path('app_bot_manager_delete', {'id': telegram_bot_integration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ telegram_bot_integration.id) }}">
    <button class="btn">Delete</button>
</form>
